[project]
name = "minimax-h3-comfyui-acceleration"
version = "0.1.0a1"
description = "Optional ComfyUI acceleration nodes and reproducible benchmark tooling for MiniMax H3"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE-APACHE-2.0.txt" }
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
